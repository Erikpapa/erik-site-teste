export interface FaqItem {
  question: string;
  answer: string;
}

export interface MethodPillar {
  step: string;
  name: string;
  description: string;
}

export interface TheologicalPrinciple {
  title: string;
  description?: string;
}
